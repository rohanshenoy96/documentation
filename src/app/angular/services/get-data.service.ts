import { Injectable } from '@angular/core';
import { HttpClient } from '@angular/common/http';

@Injectable()
export class GetDataService {
    // constructor(private http: HttpClient) {}

    getNgServeData() {
        // return this.http.get('./assets/ng-serve.data.json')
    }
}